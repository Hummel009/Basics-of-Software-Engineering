package main.java.hummel;

public class Lab1_01 {
	public static void main(String[] args) {
		double x = 0.6;
		for (int n = 10; n <= 15; n++) {
			while(x <= 1.1) {
				double firstPart = Math.cbrt(Math.exp(n*x)/2 + Math.cbrt(n*x));
				double secondPart = 0;
				for (int k = 1; k <= n; k++) {
					secondPart += Math.exp(Math.log(Math.exp(1.2 * k) + (k - 10) / (k + 30)) / k)/ Math.sqrt((k * Math.exp(Math.ln(n + 5) / 3)))+ Math.ln(Math.sqrt(n * x));
				}
				x +=0.1;
			}
		}
	}
}